// Example: Simple addition
function main() {
    let a = 15;
    let b = 27;
    let sum = a + b;
    
    // Print result as ASCII character (if it's printable)
    // or just use it to demonstrate calculation.
    // For 42 ('*'), we can see it in the output.
    print(sum); 
    halt;
}
